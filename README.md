# cua-privacy-layer
A lightweight on-device privacy layer for computer-using agents.  Cloud-based CUA models are limited in sensitive workflows because they often require access to private user data. The goal is to make CUAs safer and more usable across privacy-sensitive tasks, expanding where they can be deployed without exposing raw personal data.
